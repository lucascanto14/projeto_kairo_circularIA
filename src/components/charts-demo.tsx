import { Bar, DemoBadge, SectionTitle } from "@/components/circular-ui";

export function DestinationChart(){return <div className="rounded-lg border border-border bg-card p-5"><SectionTitle aside={<DemoBadge/>}>Destino dos ativos</SectionTitle><div className="grid min-h-48 grid-cols-3 items-end gap-5 border-b border-border px-3 pt-8">{[["Segunda vida",42],["Remanufatura",31],["Reciclagem",27]].map(([n,v])=><div key={n as string} className="flex h-full flex-col justify-end text-center"><span className="mb-2 font-display text-xl font-bold">{v}%</span><div className="mx-auto w-full max-w-20 rounded-t bg-primary" style={{height:`${Number(v)*2.4}px`,opacity:.55+Number(v)/100}}/><span className="mt-3 min-h-9 text-xs text-muted-foreground">{n}</span></div>)}</div></div>}
export function ChemistryChart(){return <div className="rounded-lg border border-border bg-card p-5"><SectionTitle aside={<DemoBadge/>}>Oferta por química</SectionTitle><div className="space-y-5 pt-3"><Bar label="LFP" value={46}/><Bar label="NMC" value={32}/><Bar label="NCA" value={14}/><Bar label="Outros" value={8}/></div></div>}
export function TrendChart(){const vals=[22,30,38,51,62,76];return <div className="rounded-lg border border-border bg-card p-5"><SectionTitle aside={<DemoBadge/>}>Tendência de oferta</SectionTitle><div className="flex h-52 items-end gap-3 border-b border-border pt-6">{vals.map((v,i)=><div key={i} className="flex h-full flex-1 flex-col justify-end text-center"><div className="mx-auto w-full max-w-16 rounded-t bg-data-b" style={{height:`${v*2}px`}}/><span className="mt-2 text-[10px] text-muted-foreground">{2026+i}</span></div>)}</div><p className="mt-4 text-xs text-muted-foreground">Previsões baseadas em IA serão aprimoradas conforme a plataforma acumular dados históricos.</p></div>}
